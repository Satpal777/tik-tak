{
  "name": "@tik-tak/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tik-tak/game-engine": "workspace:*",
    "@tik-tak/shared": "workspace:*",
    "@tik-tak/types": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260516.0",
    "typescript": "~5.8.3",
    "wrangler": "^4.15.2"
  }
}
